#include "main.h"
#include <stdio.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>
/**
 * _strchr - locates a character in a string
 * @s: input
 * @c: input
 * Return: 0 (Success)
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}

#include "main.h"
#include <stdio.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: The string
 * Return: The length
 */
int _strlen(char *s)
{
	return (strlen(s));
}

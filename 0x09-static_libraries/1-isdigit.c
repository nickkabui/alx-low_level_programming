#include "main.h"
#include <stdio.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>
/**
 * _isdigit - Checks for a digit
 * @c: The digit to be checked
 *
 * Return: 1 if Success, 0 if anything else
 */
int _isdigit(int c)
{
	return (isdigit(c));
}

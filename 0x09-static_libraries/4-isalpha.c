#include "main.h"
#include <stdio.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>
/**
 * _isalpha - checks for alphabetic character
 * @c: The character to be checked
 * Return: 1 if letter is lowercase or 0 if anything else
 */
int _isalpha(int c)
{
	return (isalpha(c));
}

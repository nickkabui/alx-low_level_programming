#include "main.h"
#include <stdio.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>
/**
 * _puts - Prints a string, followed by a new line, to stdout
 *
 * @s: The string
 */
void _puts(char *s)
{
	puts(s);
}
